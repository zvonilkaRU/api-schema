get:
  tags:
    - invites
  summary: List incoming invites
  description: Direct invites addressed to the current user.
  operationId: listIncomingInvites
  security:
    - bearerAuth: []
  parameters:
    - $ref: "../../../../../../common/src/openapi/parameters/list.yaml#/pageSizeParam"
    - $ref: "../../../../../../common/src/openapi/parameters/list.yaml#/pageTokenParam"
  responses:
    "200":
      description: Incoming invites.
      content:
        application/json:
          schema:
            $ref: "../../../schemas/models/IncomingInviteList.yaml"
    "401":
      description: Unauthorized.
      content:
        application/json:
          schema:
            $ref: "../../../../../../common/src/openapi/schemas/errors/Error.yaml"

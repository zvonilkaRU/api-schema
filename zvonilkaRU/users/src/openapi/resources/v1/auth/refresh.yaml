post:
  tags:
    - auth
  summary: Refresh access token
  description: Принимает refresh токен (httpOnly cookie zvonilka_refresh; на время миграции — также в теле), выдаёт новый access + новый refresh. Ротация — новый refresh выставляется cookie, старый инвалидируется. При reuse отозванного токена — инвалидация всей цепочки (409).
  operationId: refreshToken
  requestBody:
    required: false
    content:
      application/json:
        schema:
          $ref: "../../../schemas/auth/RefreshRequest.yaml"
  responses:
    "200":
      description: New token pair.
      content:
        application/json:
          schema:
            $ref: "../../../schemas/auth/RefreshResponse.yaml"
    "401":
      description: Invalid or expired refresh token.
      content:
        application/json:
          schema:
            $ref: "../../../../../../common/src/openapi/schemas/errors/Error.yaml"
    "409":
      description: Refresh token reuse detected. Entire chain invalidated. Client must re-login.
      content:
        application/json:
          schema:
            $ref: "../../../../../../common/src/openapi/schemas/errors/Error.yaml"
    "422":
      description: Validation error.
      content:
        application/json:
          schema:
            $ref: "../../../../../../common/src/openapi/schemas/errors/Error.yaml"
    "500":
      description: Internal error.
      content:
        application/json:
          schema:
            $ref: "../../../../../../common/src/openapi/schemas/errors/Error.yaml"

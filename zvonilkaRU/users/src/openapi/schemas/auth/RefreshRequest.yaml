name: RefreshRequest
description: Тело запроса POST /auth/refresh. Основной путь — httpOnly
  cookie zvonilka_refresh; поле в теле — откат на время миграции (ci#4).
type: object
properties:
  refresh_token:
    type: string
    description: Ранее выданный refresh токен.
    x-validations: ["Size >=1"]
additionalProperties: false

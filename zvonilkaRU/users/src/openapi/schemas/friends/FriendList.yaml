name: FriendList
description: Список друзей (accepted) с пагинацией.
type: object
required:
  - items
properties:
  items:
    type: array
    description: Друзья текущей страницы. Элементы — UserRef.
    items:
      $ref: "../models/UserRef.yaml"
  next_page_token:
    type: string
    description: Токен следующей страницы. Пустой или отсутствует — это последняя страница.
    x-validations: ["Size <=256"]
additionalProperties: false
